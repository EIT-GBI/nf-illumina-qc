// Minimal test profile — overrides params with small inputs for CI / smoke runs.

params {
    // TODO: point at a tiny test input (e.g. a public URL or assets/ file)
    input  = null
    outdir = 'results-test'
}
